/**
 * Booleans usually have two values. But it is not always the case that you have undefined or null. 
 * The use of prefixes such as is, has, can. It can help a lot to make the variable make much more semantic sense on its own.
 * 
 */


// mal - bad

const open = true;
const write = true;
const fruit = true;
const active = false;
const noValues = true;
const notEmpty = true;

// mejor - better

const isOpen = true;
const canWrite = true;
const hasFruit = true;
const isActive = false;
const hasValues = true;
const isEmpty = true;
